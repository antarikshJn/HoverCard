import React from 'react'
import PropTypes from 'prop-types'

const HoverCardTrigger = ({ children }) => {
    return (
        <div className='hoverCardTrigger'>{children}</div>
    )
}

HoverCardTrigger.propTypes = {}

export default HoverCardTrigger